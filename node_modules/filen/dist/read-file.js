"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReadFile = ReadFile;
const fs_extra_1 = require("fs-extra");
const path_1 = __importDefault(require("path"));
/**
 * Read file with asyncrounus
 * @param file_name
 * @returns
 */
function ReadFile(file_name_1) {
    return __awaiter(this, arguments, void 0, function* (file_name, { parseToJson } = { parseToJson: false }) {
        try {
            if (typeof file_name !== "string") {
                file_name = path_1.default.join(...file_name);
            }
            const file = yield (0, fs_extra_1.readFile)(file_name, "utf-8");
            // parseToJson is selected
            if (parseToJson)
                return JSON.parse(file);
            // returning reading file content.
            return file;
        }
        catch (e) {
            console.error(`${file_name} dosyası okunurken bir sorun meydana geldi!`, e);
        }
    });
}
