"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MakeDirs = MakeDirs;
const fs_extra_1 = require("fs-extra");
const path_1 = __importDefault(require("path"));
/**
 * Create Directory
 * make dir with mkdir [dir_name]
 * @param dir_name
 */
function MakeDirs(dir_name_1) {
    return __awaiter(this, arguments, void 0, function* (dir_name, { noWarnings } = { noWarnings: false }) {
        if (typeof dir_name !== "string") {
            dir_name = path_1.default.resolve(path_1.default.join(...dir_name));
        }
        try {
            yield (0, fs_extra_1.mkdirs)(dir_name)
                .catch((e) => console.error(`Error: Don't created the ${dir_name} directory!`, e));
        }
        catch (e) {
            console.error(`Error: Don't created the ${dir_name} directory!`, e);
        }
    });
}
