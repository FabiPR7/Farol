"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WriteFile = WriteFile;
const fs_extra_1 = require("fs-extra");
const path_1 = __importDefault(require("path"));
/**
 * Write file with asyncrounus
 * @param {FileNameType} file_name Can you specify file name please.
 * @param {FileContentType} file_content Can your specify file content please?
 * @param {WriteFileOptions} {options} Choose You'd like options.
 * @returns
 */
function WriteFile(file_name_1, file_content_1) {
    return __awaiter(this, arguments, void 0, function* (file_name, file_content, { noWarnings } = { noWarnings: false }) {
        try {
            if (typeof file_name !== "string") {
                file_name = path_1.default.join(...file_name);
            }
            yield (0, fs_extra_1.writeFile)(file_name, file_content, "utf-8");
        }
        catch (e) {
            if (!noWarnings)
                console.error(`${file_name} dosyasına veri işlenirken bir sorun meydana geldi!`, e);
        }
    });
}
